define(['Test'], function(Test) {
    'use strict';

    var example;
    beforeEach(function() {
        example = new Test();5
    });

    describe('Unit tests goes here', function() {
        it('should have a method named "test"', function() {
            //expect(example.add).to.be.a('function');
        });

       
    });
});